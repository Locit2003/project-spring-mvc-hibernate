<beans:beans
	xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans  
    http://www.springframework.org/schema/beans/spring-beans.xsd  
    http://www.springframework.org/schema/security  
    http://www.springframework.org/schema/security/spring-security-5.4.xsd">
	<http auto-config="true">
		<csrf disabled="true" />
		<intercept-url pattern="/admin*" access="hasRole('admin')" />

		<intercept-url pattern="/*" access="hasRole('admin') or hasRole('user')" />

		<form-login login-page="/auth/login-admin" login-processing-url="/j_spring_security_login"
			default-target-url="/admin-home"
			authentication-failure-url="/auth/login-admin"
			username-parameter="username" password-parameter="password" />
		<logout logout-url="/logout-admin"
			logout-success-url="/auth/login-admin" delete-cookies="JSESSIONID" />
	</http>
	<authentication-manager>
		<authentication-provider>
			<password-encoder ref="passwordEncoder" />
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="select username,password, enabled from users where username=?"
				authorities-by-username-query="select users.username, role.name from users, role, users_roles
				where username =? and users.id = users_roles.userId and users_roles.roleId = role.id " />
		</authentication-provider>

	</authentication-manager>
</beans:beans>  



